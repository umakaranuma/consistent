// All gym-mode profiles share the same unified daily routine
// (desk job + evening gym 6:30–8 PM). Calorie/macro targets still
// differ per gym goal — they are computed from the profile, not the plan.
import 'default_plan_normal.dart';

const List<Map<String, dynamic>> planGymFatLoss = consistentDailyPlan;
const List<Map<String, dynamic>> planGymMuscleGain = consistentDailyPlan;
const List<Map<String, dynamic>> planGymMaintenance = consistentDailyPlan;
const List<Map<String, dynamic>> planGymRecomp = consistentDailyPlan;
